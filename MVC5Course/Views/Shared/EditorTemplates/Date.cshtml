@model DateTime?

@if (Model.HasValue){
    @Html.TextBox("",Model.Value.ToString("yyyy-MM-dd"),new { @calss=" form-controll",type = "date" })
}
else
{
    @Html.TextBox("", "", new { @calss = " form-controll", type = "date" })
}
